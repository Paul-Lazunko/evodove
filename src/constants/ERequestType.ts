export enum ERequestType {
  ACCEPT ='accepted',
  STREAM_START ='streamStart',
  STREAM_END ='streamEnd',
  STREAM_CHUNK ='streamChunk',
  REQUEST ='request',
  PUBLISH ='publish',
  RESPONSE = 'response',
  SUBSCRIBE = 'subscribe',
  LISTEN = 'listen',
  SETUP = 'setup'
}
